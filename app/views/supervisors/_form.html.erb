<hr><br>
<%= form_for(@supervisor) do |f| %>
  <% if @supervisor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@supervisor.errors.count, "error") %> prohibited this supervisor from being saved:</h2>

      <ul>
      <% @supervisor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
     <div class="patient_text">کلینیک:</div><br>
    <div class="wrap"><%= f.select :clinic_id, Clinic.all.collect {|x| [x.name, x.id]}, {}, :multiple => false %></div>
  </div>

  <div class="field">
     <div class="patient_text">انتخاب مدیر:</div><br>
    <div class="wrap"><%= f.select :user_id, User.all.collect {|x| [x.profile.name , x.id]}, {}, :multiple => false %></div>
  </div>
  <!--<div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>-->
  <!--<div class="field">
    <%= f.label :clinic_id %><br>
    <%= f.number_field :clinic_id %>
  </div>-->
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
